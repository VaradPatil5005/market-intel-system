# =========================================================================
# Market Intelligence System v2 — Dependencies
# =========================================================================

# --- Core framework ---
langgraph>=0.1.0
langchain>=0.2.0
langchain-openai>=0.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- Database ---
sqlalchemy>=2.0.0

# --- HTTP / Feeds ---
requests>=2.31.0
feedparser>=6.0.0
tenacity>=8.2.0

# --- NLP & ML (all optional — pipeline degrades gracefully if absent) ---
transformers>=4.35.0
torch>=2.0.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
scipy>=1.11.0

# --- Vector store (optional — falls back to TF-IDF) ---
chromadb>=0.4.0

# --- CrewAI multi-agent framework (optional) ---
crewai>=0.1.0

# --- UI ---
streamlit>=1.28.0
pandas>=2.0.0

# --- Exports ---
openpyxl>=3.1.0

# --- Alerting ---
# (uses built-in smtplib for email; requests for Slack/Telegram)

# =========================================================================
# V2: Advanced Feature Dependencies
# =========================================================================

# --- Price data (optional — free, no key required) ---
# Install with: pip install yfinance
yfinance>=0.2.30

# --- Live daemon scheduler (optional) ---
# Install with: pip install apscheduler
apscheduler>=3.10.0

# --- Deep search alternatives (optional) ---
# Tavily (premium): pip install tavily-python
# DuckDuckGo is accessed via requests (already installed, no extra package)

# --- Playwright (optional — JS-heavy site scraping) ---
# Install with: pip install playwright && playwright install chromium
# playwright>=1.40.0

# --- OpenAI SDK (optional — for LLM-quality Reflexion lessons) ---
openai>=1.0.0

# --- Anthropic SDK (optional) ---
anthropic>=0.20.0
